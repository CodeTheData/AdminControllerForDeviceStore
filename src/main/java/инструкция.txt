Теория:
"JDBC"
1. Создание проекта и класса для подключения к БД ( Connection )
/*
Через DriverManager - DriverManager позволяет подключиться к базе данных по указанному URL,
а так же загружает JDBC Driver'ы, который он нашёл в CLASSPATH
(а раньше, до JDBC 4.0 загружать класс драйвера надо было самостоятельно).
*/
2. Прокидывание Connection в другие классы, которые работают с SQL запросами
3. Написание SQL запросов под нужды проекта
========================================================================================================================
"HIBERNATE"
/*
JDBC - Java DataBased Connectivity (это было открытием, но проблема была в большом количестве подключений к базе,
т.к. база не понимала к какому объекту она относится.

ORM - Object relational mapping технология, которая позволяет нам представлять обьекты в виде реляционной бд.

Hubernate - конкретная библиотека, в которой содержится реализация ORM
(реализует маппинг объекта и строк в базе) - соответствует JPA.

JPA - Java Persistence API спецификация ( некоторое правило ) ( стандарт ) для реализации принципов ORM для джавы.

JDBC - HIBERNATE
( session -> connection )
( Connection pool -> SessionFactory )
( application.properies -> hibernate.cfg.xml)

Последовательность:
1. Создание проекта и подключение зависимостей
2. Добавление параметров для работы hibernate
3. Mapping классов для связывания таблиц БД с java кодом
4. Создание нужных SQL запросов
5. Обработка результатов
6. Предоставление результатов в GUI
*/
========================================================================================================================

Что сделал:
-- изучил JDBC:

-- сделал Регистрацию клиента

-- Исправил ошибку в методе update, забыл пробел перед WHERE!

-- Начал изучать hibernate

-- Залил код в отдельную ветку + создал репозиторий

Что понял:
Как правильно юзать сканнер(что открываешь в мейне и только в конце закрываешь)
т.е. в каждом методе не надо!

Что нужно сделать:
-- изучить ORM

-- Показ чека, сразу при продаже!

-- Как проверять есть ли айди в таблице?


